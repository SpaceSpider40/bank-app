import React from "react";
import { Navigation } from "../../components/navbar";

export const DesktopPage:React.FunctionComponent = (props:any) => {
    return <>
        <Navigation/>
    DESKTOP
    </>
}